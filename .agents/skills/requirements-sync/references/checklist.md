# Requirements Sync Checklist

- FR IDs in summary and detailed sections stay aligned.
- SCR IDs referenced in transitions exist in wireframe files.
- BR IDs referenced in feature details have consistent behavior statements.
- User roles (`U-*`) are consistent with feature ownership.
- Decision-log updates are added when policy or scope changed.
